/* import React from 'react';
import Menu from '../components/Menu';
import { render, fireEvent } from '@testing-library/react';
import { act } from 'react-dom/test-utils';


describe('Menu', () => {
    it('Deberia mostar la lista de menu por categoria',() => {
        const addProduct = jest.fn();
        const totalClient = jest.fn();
        //const obj1 = jest.fn();
       
      
    const container = render(<Menu addProduct= {addProduct} totalClient={totalClient} />)
    const button = container.getByText('DESAYUNO');
       
    //const listaDeNodos = container.getAllByTestId('IdItem');
    // dar click al boton
    act(() => {
        fireEvent.click(button);
      })
      
    
    }
    
    )
}

) */